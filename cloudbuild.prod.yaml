steps:
  # build & push the container image
- name: 'gcr.io/cloud-builders/docker'
  entrypoint: 'bash'
  args: ['-c', 'docker pull gcr.io/people2-admin/anymind:latest || exit 0']
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/people2-admin/anymind:latest', '--cache-from', 'gcr.io/people2-admin/anymind:latest', '.', '--file', './Dockerfile.prod']
  # Push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/people2-admin/anymind:latest']
  # Deploy container image to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args: ['run', 'deploy', 'anymind-api', '--image', 'gcr.io/people2-admin/anymind:latest', '--memory', '1Gi', '--concurrency', '80', '--max-instances', '100', '--region', 'us-central1', '--allow-unauthenticated', '--platform', 'managed']
images:
- gcr.io/people2-admin/anymind:latest
timeout: 3600s